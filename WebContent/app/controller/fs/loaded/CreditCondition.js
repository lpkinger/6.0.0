Ext.QuickTips.init();
Ext.define('erp.controller.fs.loaded.CreditCondition', {
	extend : 'Ext.app.Controller',
	FormUtil : Ext.create('erp.util.FormUtil'),
	views : ['core.grid.Panel2','fs.loaded.CreditCondition'],
	init : function() {
		var me = this;
		this.control({
			
		})
	}
});